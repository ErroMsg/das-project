#include "errorcode.h"

const char *getErrorDetail(int errorCode)
{
    switch (errorCode) {
    case DAS_USER_LOGIN_ERR:

        break;
    default:
        break;
    }

    return "unknown error";
}
